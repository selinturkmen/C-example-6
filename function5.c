#include <funstion5.h>
#include <stdio.h>
#include <math.h>

void FronormMatrix(const double *AAT, int nrows, int ncols){
	for (i =0; i<nrows; ++i){
		int fronorm;
		fronorm += AAT[nrows][ncols];
		return sqrt(fronorm));}
